import Base from "../components/Base";
import CustomNavbar from "../components/CustomNavbar";

const About=()=>{
    return(
        <Base>
            <h1>This is our about page</h1>
            <p>we are building blogging app</p>
        </Base>
    );
};

export default About;